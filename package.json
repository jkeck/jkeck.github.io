{
  "name": "jessiekeck-comments",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:unit": "vitest run test/unit",
    "test:int": "vitest run test/integration",
    "typecheck": "tsc --noEmit",
    "vendor:build": "node scripts/build-vendor.js",
    "db:start": "supabase start",
    "db:reset": "supabase db reset"
  },
  "devDependencies": {
    "@supabase/supabase-js": "^2.108.2",
    "esbuild": "^0.25.0",
    "jsdom": "^26.0.0",
    "typescript": "^5.0.0",
    "vitest": "^3.0.0"
  }
}
